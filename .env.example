# Copy to `.env` next to docker-compose.yml on the server (gitignored).
# docker compose reads it automatically.

# Auth secret for admin JWT
JWT_SECRET=change-me-to-a-long-random-string

# Telegram lead notifications — shared bot @termojet_ua_bot.
# Заявки з форм (консультація / замовлення / дилер) падатимуть у цей чат.
# Якщо змінні порожні — сповіщення просто вимкнені.
#   TELEGRAM_BOT_TOKEN — токен бота від @BotFather
#   TELEGRAM_CHAT_ID   — чат призначення; група Termojet Sales = -1003809508040
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=-1003809508040
